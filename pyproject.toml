[project]
name = "agent-harness-lab"
version = "0.1.0"
description = "A TDD-first Agent Harness prototype for reproducible AI agent workflows."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
